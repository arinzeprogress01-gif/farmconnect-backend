| `express.json()`       | Reads JSON request bodies                   | Production                                             |
| `express.urlencoded()` | Reads HTML form data                        | Production                                             |
| `helmet()`             | Adds security HTTP headers                  | Production                                             |

| `cors()`               | Allows frontend to communicate with backend | Production                                             |
| `morgan()`             | Logs incoming HTTP requests                 | Development (sometimes production with custom logging) |