const farmConnectNotifications = `

# FARMCONNECT — NOTIFICATIONS MEMORY

## 1. NOTIFICATION DOMAIN

FarmConnect has a persistent notification system used to communicate important application events to users.

Notifications are generated by backend business operations such as:

- vendor profile creation
- vendor profile updates
- listing publication
- listing updates
- listing cancellation
- listing expiration
- new reservations
- reservation confirmation
- reservation cancellation
- reservation completion
- reservation expiration
- password/security events
- system events

Notifications are therefore connected directly to FarmConnect's business lifecycle.

The notification system is not merely a messaging feature.

It provides users with actionable information about changes occurring in the marketplace.

---

# 2. NOTIFICATION OWNERSHIP

Every notification belongs to a receiver.

The receiver represents the User who should receive the notification.

Conceptually:

BUSINESS EVENT
    ↓
NOTIFICATION
    ↓
USER / RECEIVER

The notification system does not require the receiver to be the person who initiated the event.

For example:

USER creates reservation
    ↓
VENDOR receives "New Reservation"

and:

USER creates reservation
    ↓
USER receives "Reservation Confirmed"

Therefore, one business event can generate notifications for multiple users.

---

# 3. NOTIFICATION DATA

A notification contains information including:

- receiver
- title
- message
- type
- isRead
- priority
- data
- createdAt
- updatedAt

The notification data is designed to provide both human-readable information and structured information that the frontend can use.

---

# 4. NOTIFICATION TITLE

The title provides a short description of the event.

Examples include:

"Listing Published"

"Listing Updated"

"Listing Cancelled"

"Listing Expired"

"New Reservation"

"Reservation Confirmed"

"Reservation Cancelled"

"Reservation Completed"

"Reservation Expired"

"Vendor Profile Created"

"Vendor Profile Updated"

"Vendor Profile Deleted"

The title should clearly communicate what happened.

---

# 5. NOTIFICATION MESSAGE

The message provides the user with a human-readable explanation of the event.

Messages may include contextual information such as:

- food name
- quantity
- vendor
- user
- reservation
- pickup code
- cancellation reason
- expiration information

The message should describe the actual backend event.

Mini Farm AI should not invent notification events that are not supported by the system.

---

# 6. NOTIFICATION TYPES

FarmConnect uses notification types to categorize events.

Known notification types include:

vendor

listing

listing_created

listing_updated

listing_cancelled

listing_completed

listing_expired

reservation

reservation_created

reservation_completed

reservation_cancelled

vendor_profile_created

password_reset

system

reminder

promotion

security

The exact type used depends on the business operation generating the notification.

---

# 7. NOTIFICATION PRIORITY

Notifications have priority levels:

low

medium

high

Priority communicates the importance of the event.

Examples:

Profile update:
    low

Listing expiration:
    medium

New reservation:
    high

Reservation confirmation:
    high

Reservation cancellation:
    high

The priority should reflect the importance of the event rather than simply the existence of the notification.

---

# 8. READ STATE

Notifications maintain a read state.

The important state is:

isRead

A notification is initially unread when it is created.

Users can mark individual notifications as read.

Users can also mark all of their notifications as read.

This allows the application to distinguish:

UNREAD NOTIFICATIONS
from
READ NOTIFICATIONS

---

# 9. GETTING USER NOTIFICATIONS

Users can retrieve their own notifications.

The notification service uses:

getUserNotifications(userId)

The authenticated user's ID determines which notifications are returned.

A user should not receive another user's private notifications.

---

# 10. GETTING UNREAD NOTIFICATIONS

FarmConnect provides a dedicated operation for unread notifications.

The service uses:

getUnreadNotifications(userId)

This allows the frontend to display information such as:

- unread notification lists
- notification badges
- notification counters
- notification indicators

The unread count/list should be based on actual stored notification state.

---

# 11. GETTING NOTIFICATION DETAILS

A user can request a specific notification.

The system:

1. Finds the notification by ID.
2. Confirms the notification exists.
3. Verifies that the notification receiver matches the authenticated user.
4. Returns the notification.

If the notification does not exist:

"Notification not found."

If the notification belongs to another user:

"You are not allowed to view this notification."

---

# 12. NOTIFICATION AUTHORIZATION

Notifications are private user data.

The system checks:

notification.receiver
    ===
authenticated userId

before allowing the user to view or modify a notification.

A user cannot access another user's notification simply by knowing its notification ID.

This is an important authorization rule.

---

# 13. MARKING A NOTIFICATION AS READ

Users can mark an individual notification as read.

The service:

1. Finds the notification.
2. Confirms it exists.
3. Verifies ownership.
4. Sets:

isRead = true

5. Saves the notification.

The user can therefore update only their own notifications.

---

# 14. MARKING ALL NOTIFICATIONS AS READ

FarmConnect also supports marking every notification belonging to a user as read.

The operation uses:

markAllAsRead(userId)

This applies to the authenticated user's notifications.

It does not mark notifications belonging to other users as read.

---

# 15. NOTIFICATION ACTION DATA

Notifications can contain structured data describing what the frontend should open when the user interacts with the notification.

Examples include:

action = "OPEN_HOME"

action = "OPEN_MY_LISTINGS"

action = "OPEN_MY_RESERVATIONS"

action = "OPEN_VENDOR_RESERVATIONS"

action = "OPEN_VENDOR_PROFILE"

action = "OPEN_PROFILE"

The data can also contain identifiers such as:

listingId

reservationId

vendorId

This creates a connection between backend business events and frontend navigation.

---

# 16. NOTIFICATION NAVIGATION

A notification can conceptually work as:

NOTIFICATION
    ↓
USER OPENS NOTIFICATION
    ↓
READ NOTIFICATION DATA
    ↓
ACTION
    ↓
OPEN RELEVANT SCREEN

For example:

New Reservation
    ↓
action = OPEN_VENDOR_RESERVATIONS
    ↓
Vendor reservation screen

Reservation Confirmed
    ↓
action = OPEN_MY_RESERVATIONS
    ↓
User reservation screen

Listing Published
    ↓
action = OPEN_MY_LISTINGS
    ↓
Vendor listings screen

Vendor Profile Created
    ↓
action = OPEN_VENDOR_PROFILE
    ↓
Vendor profile screen

---

# 17. LISTING NOTIFICATIONS

Listing-related events can generate notifications.

Examples:

## Listing Published

When a vendor successfully creates a listing, the vendor receives:

Title:
"Listing Published"

Message identifies the published food.

Data includes:

listingId

action:
"OPEN_MY_LISTINGS"

---

# 18. LISTING UPDATED

When a vendor updates a listing, the vendor receives:

Title:

"Listing Updated"

The message identifies the updated food.

Data contains:

listingId

action:
"OPEN_MY_LISTINGS"

---

# 19. LISTING CANCELLED

When a vendor removes/cancels their listing, the vendor receives:

Title:

"Listing Cancelled"

The message explains that the food has been removed from the marketplace.

Data contains:

listingId

action:
"OPEN_MY_LISTINGS"

---

# 20. LISTING EXPIRED

When the listing expiration scheduler detects an expired listing:

listing.status = expired

listing.isActive = false

The vendor receives:

Title:

"Listing Expired"

The message explains that the food listing has expired and is no longer visible in the marketplace.

Data contains:

listingId

The expiration notification is generated by the background automation system.

---

# 21. VENDOR PROFILE NOTIFICATIONS

Vendor profile operations can generate notifications.

## Profile Created

When a vendor successfully creates their vendor profile:

Title:

"Vendor Profile Created"

The message congratulates the vendor and informs them that they can now publish food listings.

Data contains:

vendorId

action:
"OPEN_VENDOR_PROFILE"

---

# 22. PROFILE UPDATED

When the vendor profile is successfully updated:

Title:

"Vendor Profile Updated"

The message confirms that the profile was updated.

Data contains:

vendorId

action:
"OPEN_VENDOR_PROFILE"

---

# 23. PROFILE DELETED

When a vendor profile is deleted:

Title:

"Vendor Profile Deleted"

The message confirms that the profile has been removed.

Data contains:

action:
"OPEN_HOME"

The frontend can therefore return the user to the home screen.

---

# 24. RESERVATION NOTIFICATIONS

Reservation operations generate notifications for both users and vendors.

A single reservation event may therefore create multiple notifications.

The notification receiver depends on who needs to know about the event.

---

# 25. NEW RESERVATION — VENDOR

When a user successfully reserves food:

The vendor receives:

Title:

"New Reservation"

The message identifies:

- user
- quantity
- food name

Data contains:

reservationId

listingId

action:
"OPEN_VENDOR_RESERVATIONS"

This tells the vendor that a new reservation requires attention.

---

# 26. RESERVATION CONFIRMED — USER

After successful reservation creation, the user receives:

Title:

"Reservation Confirmed"

The message identifies:

- food name
- pickup code

Data contains:

reservationId

action:
"OPEN_MY_RESERVATIONS"

The pickup code is included because it is important for the user's reservation/pickup process.

---

# 27. LISTING FULLY RESERVED

When a reservation consumes the final available quantity:

listing.quantity = 0

and:

listing.status = pendingCompletion

the vendor receives:

Title:

"Listing Fully Reserved"

The message informs the vendor that the listing has been fully reserved and is awaiting reservation completion.

Data contains:

listingId

action:
"OPEN_MY_LISTINGS"

---

# 28. RESERVATION CANCELLED BY VENDOR

When a vendor cancels a reservation, the affected user receives:

Title:

"Reservation Cancelled"

The message contains the cancellation reason.

Data contains:

reservationId

action:
"OPEN_MY_RESERVATIONS"

This notification is important because the user needs to know that their previously active reservation is no longer valid.

---

# 29. RESERVATION CANCELLED BY USER

When a user cancels their reservation:

The vendor receives:

Title:

"Reservation Cancelled"

Data contains:

reservationId

The user also receives:

Title:

"Reservation Cancelled"

The message confirms that their reservation was successfully cancelled.

Data contains:

reservationId

Therefore, both parties are informed about the cancellation.

---

# 30. RESERVATION COMPLETED

When a vendor successfully completes a reservation:

The user receives:

Title:

"Reservation Completed"

The message confirms that the reservation has been completed.

Data contains:

reservationId

action:
"OPEN_MY_RESERVATIONS"

The notification represents successful fulfilment.

---

# 31. RESERVATION EXPIRED

When the reservation expiration scheduler detects an expired reservation:

The user receives:

Title:

"Reservation Expired"

The message explains that the reservation for the food has expired.

Data contains:

reservationId

The vendor also receives:

Title:

"Reservation Expired"

The message explains that the reservation expired and the quantity has been restored.

Data contains:

reservationId

---

# 32. PASSWORD RESET NOTIFICATIONS

The notification architecture also supports password/security-related events.

Password-reset operations may involve:

- OTP delivery
- password reset confirmation
- push notification
- security notifications

The notification system is therefore not limited to marketplace events.

---

# 33. DEVICE AND PUSH NOTIFICATIONS

FarmConnect supports registered user devices.

A device can contain information such as:

- push token
- platform
- browser
- lastSeen

Stored notifications and push notifications are conceptually separate.

The persistent notification record remains part of the FarmConnect database even when push delivery is unavailable.

---

# 34. PERSISTENT NOTIFICATION VS PUSH DELIVERY

The notification architecture can be understood as:

BUSINESS EVENT
    ↓
CREATE PERSISTENT NOTIFICATION
    ↓
OPTIONALLY DELIVER PUSH
    ↓
USER CAN VIEW NOTIFICATION IN APP

Push delivery should not be treated as the only record of an event.

The stored notification provides the durable application history.

---

# 35. NOTIFICATION SERVICE PATTERN

Business services generate notifications through:

sendNotification(...)

or the underlying notification repository:

createNotification(...)

This keeps notification creation connected to business operations.

Examples:

Listing service
    ↓
sendNotification()
    ↓
Notification

Reservation service
    ↓
sendNotification()
    ↓
Notification

Vendor service
    ↓
sendNotification()
    ↓
Notification

Automation
    ↓
createNotification() / sendNotification()
    ↓
Notification

---

# 36. NOTIFICATION REPOSITORY

The notification repository provides operations such as:

findNotificationById

getUserNotifications

getUnreadNotifications

updateNotification

markAllAsRead

createNotification

The service layer applies business rules and authorization before repository operations are performed.

---

# 37. NOTIFICATION SERVICE RESPONSIBILITIES

The notification service is responsible for:

- retrieving user notifications
- retrieving unread notifications
- retrieving notification details
- verifying notification ownership
- marking individual notifications as read
- marking all user notifications as read

The service should not allow users to bypass ownership checks.

---

# 38. NOTIFICATION SECURITY

The most important security rule is:

A notification belongs only to its receiver.

Therefore:

GET NOTIFICATION
    ↓
FIND NOTIFICATION
    ↓
VERIFY receiver === userId
    ↓
RETURN

If the receiver does not match:

FORBIDDEN

The same ownership rule applies when updating notification state.

---

# 39. NOTIFICATION BUSINESS FLOW

The general notification lifecycle is:

BUSINESS EVENT
    ↓
BUSINESS SERVICE
    ↓
NOTIFICATION CREATED
    ↓
RECEIVER STORED
    ↓
USER SEES NOTIFICATION
    ↓
USER MAY OPEN ACTION
    ↓
NOTIFICATION MAY BE MARKED READ

---

# 40. NOTIFICATION READ FLOW

The read lifecycle is:

UNREAD
    ↓
USER OPENS / MARKS NOTIFICATION
    ↓
isRead = true
    ↓
READ

Users can also perform:

MARK ALL AS READ
    ↓
all notifications belonging to user
    ↓
isRead = true

---

# 41. NOTIFICATION OWNERSHIP RULE

Mini Farm AI must understand:

A notification ID alone does not grant access.

The system must verify:

notification.receiver === authenticated user

Therefore:

"Show me notification 123"

does not mean the user can see notification 123.

The notification must belong to that user.

---

# 42. IMPORTANT AI INTERPRETATION RULES

Mini Farm AI should understand notification-related questions such as:

"Do I have any notifications?"

    ↓
Retrieve the user's notifications.

"Do I have unread notifications?"

    ↓
Retrieve unread notifications for that user.

"Mark this notification as read."

    ↓
Verify ownership.
    ↓
Set isRead = true.

"Mark all my notifications as read."

    ↓
Mark all notifications belonging to authenticated user as read.

"What does this notification mean?"

    ↓
Interpret the stored title, message, type and data.

"Why did I get this notification?"

    ↓
Explain the actual backend event represented by the notification.

---

# 43. NOTIFICATION DATA MUST NOT BE INVENTED

Mini Farm AI must not fabricate:

- notification titles
- notification types
- reservation IDs
- listing IDs
- vendors
- cancellation reasons
- actions
- events

If the required notification information is not supplied to the AI, it should clearly state that it does not have the relevant information.

---

# 44. NOTIFICATION → BUSINESS DOMAIN CONNECTION

Notifications are generated because something happened elsewhere in FarmConnect.

Examples:

Vendor Profile
    ↓
Profile Created
    ↓
Vendor Notification

Listing
    ↓
Published
    ↓
Listing Notification

Listing
    ↓
Expired
    ↓
Expiration Notification

Reservation
    ↓
Created
    ↓
Vendor + User Notifications

Reservation
    ↓
Cancelled
    ↓
Affected Party Notifications

Reservation
    ↓
Completed
    ↓
User Notification

Reservation
    ↓
Expired
    ↓
User + Vendor Notifications

---

# 45. NOTIFICATION PRINCIPLE

The notification system should communicate actual FarmConnect state changes.

It should never be treated as an independent source of business truth.

The underlying business entity remains authoritative.

For example:

Notification:
"Reservation Completed"

The actual reservation record remains the source of truth for:

reservation.status

Similarly:

Notification:
"Listing Expired"

The listing record remains the source of truth for:

listing.status
listing.isActive
listing.expiresAt

Mini Farm AI should use the actual entity state whenever current truth is required.

`;

export default farmConnectNotifications;